/**
 * A renderer interface to renderer a board.
 */
public interface Renderer {
    /**
     * Renderers a given board.
     * @param board to renderer.
     */
    void renderBoard(Board board);
}
